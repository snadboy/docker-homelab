# Required: Random 32-character salt for settings encryption
# Generate with: openssl rand -base64 32
SALT=CHANGE_ME_32_CHARS_RANDOM_STRING

# Optional: JWT token signing secret (auto-generated if not provided)
# Generate with: openssl rand -base64 32
JWT_SECRET=

# Optional: Database encryption key (auto-generated if not provided)
# Generate with: openssl rand -base64 32
DATABASE_KEY=
