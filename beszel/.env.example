# Beszel hub does not currently require any env vars at runtime.
# Admin user is created via the web UI on first launch.
# Universal token + hub SSH public key (used by agents) are generated in the
# hub UI and consumed by the beszel-agent Ansible role, not by this stack.
