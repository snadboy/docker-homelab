# Firefly III Configuration

# Site Owner Email (required)
SITE_OWNER=your-email@example.com

# App Key - Must be exactly 32 characters
# Generate with: head /dev/urandom | LC_ALL=C tr -dc 'A-Za-z0-9' | head -c 32 && echo
APP_KEY=

# Database Password
# Generate with: head /dev/urandom | LC_ALL=C tr -dc 'A-Za-z0-9' | head -c 24 && echo
DB_PASSWORD=

# Cron Token - For scheduled tasks
# Generate with: head /dev/urandom | LC_ALL=C tr -dc 'A-Za-z0-9' | head -c 32 && echo
STATIC_CRON_TOKEN=

# Data Importer Configuration

# Personal Access Token from Firefly III
# Create after first login: Profile -> OAuth -> Personal Access Tokens
IMPORTER_ACCESS_TOKEN=

# Auto Import Secret (for scheduled imports via webhook)
# Generate with: head /dev/urandom | LC_ALL=C tr -dc 'A-Za-z0-9' | head -c 32 && echo
AUTO_IMPORT_SECRET=

# SimpleFIN Token (US/Canada bank sync)
# Get from: https://beta-bridge.simplefin.org/
SIMPLEFIN_TOKEN=
